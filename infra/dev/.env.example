# Copy to `.env` (gitignored). Per ADR-0016: secrets only, no general config.
# v0.1 has no required secrets (local Ollama needs no auth). Reserved for later phases.

# Example for future use:
# OPENAI_API_KEY=sk-...

[project]
name = "web"
version = "0.0.0"
description = "Crawl4AI adapter — interface only in v0.1."
requires-python = ">=3.12"
dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/web"]

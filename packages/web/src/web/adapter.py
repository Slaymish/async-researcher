"""search(query, k) -> list[SearchHit] and fetch(url) -> MarkdownDoc.

Stub — interfaces only. v0.2 wires SearXNG + DDGS for search (ADR-0018) and Crawl4AI
for fetch. The shape here is what the rest of the system codes against; the body lands
when the v0.2 milestone starts.
"""
